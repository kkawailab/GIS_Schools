# 練習問題：愛知県学校マップの機能拡張

このドキュメントでは、愛知県学校マップアプリケーションの機能を拡張するための練習問題を提供します。各問題には、AI（ChatGPT、Claude）を活用するための効果的なプロンプトのヒントも含まれています。

## この教材の使い方

### 対象者

- HTML/CSS/JavaScriptの基礎を学び始めた方（ウォームアップ・基礎レベル）
- Webアプリ開発の実践力を付けたい方（中級レベル以上）
- AIを活用した開発（プロンプトの書き方）を練習したい方（全レベル）

### 学習の進め方

1. **まずアプリを動かす**: README.mdの起動手順に従い、地図が表示されることを確認します
2. **バックアップを取る**: 各問題に取り組む前に、作業用のGitブランチを作るか `index.html` をコピーしておくと、失敗しても元に戻せて安心です
   ```bash
   git checkout -b exercise-1   # 問題1用のブランチを作成する例
   ```
3. **問題に挑戦する**: 自力で書いてみても、AIにプロンプトを書いて任せてもOKです。ヒントのプロンプトをそのまま使わず、自分の言葉に直してみると理解が深まります
4. **動作を確認する**: ブラウザを再読み込みして動きを確認し、`F12` キーで開く開発者ツールのConsoleにエラーが出ていないかチェックします
5. **うまくいったら記録する**: `git add -A && git commit -m "問題1: フィルター機能を追加"` のようにコミットして進捗を残しましょう

### レベルと目安

| レベル | 問題 | 目安時間 | 前提知識 |
|--------|------|---------|---------|
| ウォームアップ | 問題0-1〜0-3 | 各15〜30分 | なし（コードを読んで数値や文字を変えるだけ） |
| 基礎 | 問題1〜5 | 各1〜2時間 | JavaScriptの基本（変数、関数、配列） |
| 中級 | 問題6〜8 | 各2〜4時間 | 外部ライブラリの利用、非同期処理 |
| 上級 | 問題9〜11 | 各4〜8時間 | API連携、データ変換 |
| エキスパート | 問題12〜13 | 各1日以上 | Webアプリ開発の総合力 |

---

## ウォームアップ

コードに触るのが初めての方向けの準備運動です。`index.html` をテキストエディタ（VS Codeなど）で開き、「変更 → 保存 → ブラウザ再読み込み → 確認」のサイクルに慣れましょう。

### 問題0-1: マーカーの色を変えてみる

**目標**: 公立学校のマーカーを青から好きな色に変える。

**手順**:
1. `index.html` の `getMarkerColor()` 関数を探す（エディタで `getMarkerColor` を検索）
2. `return '#3498db';` の色コードを好きな色（例: 緑 `'#2ecc71'`）に変更
3. ファイルを保存してブラウザを再読み込み

**確認**: 青かったマーカーが選んだ色に変われば成功です。凡例（画面右下）の色はHTML内の `legend-color` の `background-color` で指定されているので、そちらも合わせて変更してみましょう。

**学習ポイント**: コードの変更が画面にどう反映されるかを体験する

---

### 問題0-2: 地図の初期表示を変えてみる

**目標**: 最初に表示される場所とズームレベルを変える。

**手順**:
1. `L.map('map').setView([35.1, 137.0], 10)` の行を探す
2. `[35.1, 137.0]` は [緯度, 経度]、最後の `10` はズームレベル（数字が大きいほど拡大）
3. 名古屋駅 `[35.170694, 136.881637]`、ズームレベル `14` などに変えてみる

**確認**: 再読み込み後、名古屋駅周辺が拡大表示されれば成功です。いろいろな場所・ズームを試してみましょう。

**学習ポイント**: 緯度・経度とズームレベルの感覚をつかむ

---

### 問題0-3: ポップアップに情報を追加してみる

**目標**: マーカークリック時のポップアップに「学校種別コード（P29_003）」の行を追加する。

**手順**:
1. `popupContent` を組み立てている部分を探す
2. `<tr>...</tr>` のブロックを1つコピーして貼り付け、`${props.P29_001 || '-'}` の部分を `${props.P29_003 || '-'}` に変更
3. ラベル部分（「🗺️ 行政区域:」など）も「🏷️ 種別コード:」のように書き換える

**確認**: マーカーをクリックして、追加した行が表示されれば成功です。

**学習ポイント**: テンプレートリテラル（`${...}`）とHTMLの構造を理解する

---

## 基礎レベル

### 問題1: 学校種別フィルター機能の追加

**目標**: 幼稚園、小学校、中学校などの学校種別で表示を絞り込めるフィルター機能を追加する。

**要件**:
- 画面上にチェックボックスまたはドロップダウンで学校種別を選択できるUIを配置
- 選択した種別の学校のみを地図上に表示
- フィルター変更時にマーカーをリアルタイムで更新

**ヒント：効果的なプロンプトの書き方**:
```
「index.htmlに学校種別（P29_003コード）でフィルタリングする機能を追加してください。
以下の要件を満たすようにしてください：

1. 画面左上に学校種別を選択できるチェックボックスリストを配置
2. P29-23_23_structure.mdを参照して学校種別コードの意味を理解してください
3. チェックボックスの選択に応じてマーカーの表示/非表示を切り替える
4. 既存のコードスタイルとデザインに合わせてください
5. 初期状態ではすべての種別が選択された状態にしてください」
```

**学習ポイント**:
- Leaflet.jsのレイヤー管理
- JavaScriptのイベントハンドリング
- DOM操作とUIの動的更新

---

### 問題2: 検索機能の実装

**目標**: 学校名や住所で検索し、該当する学校にズームする機能を追加する。

**要件**:
- 検索ボックスのUIを追加
- 学校名（P29_004）または住所（P29_005）で部分一致検索
- 検索結果を地図上でハイライト表示
- 該当する学校の位置に地図を移動・ズーム

**ヒント：効果的なプロンプトの書き方**:
```
「学校名または住所で検索できる機能を実装してください。

具体的な実装要件：
1. 地図上部に検索ボックスを配置（既存のinfo-panelとデザインを統一）
2. 入力値でP29_004（学校名）とP29_005（住所）を部分一致検索
3. 検索結果リストを表示し、クリックで該当の学校にズーム
4. 該当マーカーを一時的に異なる色やサイズで強調表示
5. 検索結果が0件の場合は「該当なし」と表示

参考にするべきLeaflet.jsのAPIメソッド：
- map.setView()でズーム
- marker.setIcon()でアイコン変更」
```

**学習ポイント**:
- 配列の検索・フィルタリング（Array.filter）
- 文字列の部分一致処理
- Leaflet.jsの地図操作API

---

### 問題3: 廃校の表示切り替え機能

**目標**: 廃校（P29_007が"1"の学校）の見た目を変え、表示/非表示を切り替えられるようにする。

**要件**:
- 廃校のマーカーを通常と異なる見た目（例: 黒色・半透明）で表示
- 「廃校を表示する」チェックボックスを画面に配置
- チェックのオン/オフで廃校マーカーの表示を切り替え
- 凡例に廃校の色を追加

**ヒント：効果的なプロンプトの書き方**:
```
「廃校の表示を切り替える機能を追加してください。

要件：
1. P29_007が"1"の学校（廃校）のマーカーを黒色・透明度0.6で表示
2. 凡例パネルに「廃校を表示する」チェックボックスを追加（初期状態はオン）
3. 廃校マーカーは専用のL.layerGroup()にまとめ、
   チェックボックスのchangeイベントでmap.addLayer()/removeLayer()を実行
4. 凡例に黒色の凡例項目「廃校」を追加
5. 既存のgetMarkerColor()やポップアップの仕組みは維持してください」
```

**学習ポイント**:
- 条件分岐によるスタイルの出し分け
- L.layerGroup()によるマーカーのグループ管理
- チェックボックスのイベント処理

---

### 問題4: 設置者別の学校数を表示する

**目標**: 画面右上の情報パネルに、公立・私立・その他の学校数の内訳を表示する。

**要件**:
- GeoJSON読み込み後にP29_006ごとの件数を集計
- 情報パネルに「公立: ◯件 / 私立: ◯件 / その他: ◯件」のように表示
- 数値は3桁区切り（toLocaleString）で表示
- 文字色をマーカーの色（公立=青、私立=赤）と合わせる

**ヒント：効果的なプロンプトの書き方**:
```
「情報パネル（info-panel）に設置者別の学校数を表示してください。

要件：
1. fetchでデータを読み込んだ後、features配列をP29_006の値ごとに集計する
   countByOperator()のような関数を作成
2. "3"（公立）、"4"（私立）、それ以外（その他）の3区分で集計
3. 総施設数の下に3行追加して件数を表示
4. 公立は#3498db、私立は#e74c3c の文字色で表示し、マーカーの色と統一
5. 数値はtoLocaleString()で3桁区切りにしてください」
```

**学習ポイント**:
- Array.filter / Array.reduce によるデータ集計
- DOM操作による表示の更新
- データとUIの色の一貫性

---

### 問題5: 現在地へジャンプするボタン

**目標**: ブラウザのGeolocation APIで現在地を取得し、地図を現在地に移動するボタンを追加する。

**要件**:
- 「現在地へ移動」ボタンを地図上に配置
- クリックで現在地を取得し、map.setView()で移動
- 現在地に専用マーカー（例: 緑色）を表示
- 位置情報の取得を拒否された場合はエラーメッセージを表示

**ヒント：効果的なプロンプトの書き方**:
```
「現在地へジャンプするボタンを追加してください。

要件：
1. 画面左下に「📍 現在地へ移動」ボタンを配置（info-panelと同じデザイン）
2. クリック時にnavigator.geolocation.getCurrentPosition()で現在地を取得
3. 取得成功時：map.setView([緯度, 経度], 14)で移動し、
   緑色のマーカーを現在地に表示（2回目以降は前のマーカーを削除）
4. 取得失敗時：「位置情報を取得できませんでした」とアラート表示
   エラーの種類（許可拒否・タイムアウトなど）で文言を変えられるとなお良い
5. 位置情報はHTTPSまたはlocalhostでのみ動作する点に注意してください」
```

**学習ポイント**:
- ブラウザAPI（Geolocation API）の利用
- 成功/失敗のコールバック処理
- エラーハンドリングの基本

---

## 中級レベル

### 問題6: クラスタリング機能の追加

**目標**: ズームアウト時に近接する学校をグループ化してクラスター表示する。

**要件**:
- Leaflet.markerclusterプラグインを導入
- ズームレベルに応じて自動的にマーカーをクラスター化
- クラスターの数値表示
- クラスタークリックで拡大表示

**ヒント：効果的なプロンプトの書き方**:
```
「Leaflet.markerclusterプラグインを使用してマーカーのクラスタリング機能を実装してください。

手順：
1. CDNから以下を追加してください：
   - leaflet.markercluster.js（バージョン1.5.3推奨）
   - MarkerCluster.cssとMarkerCluster.Default.css
2. 既存のL.geoJSON()の処理をL.markerClusterGroup()に変更
3. 設置者別の色分けは維持してください
4. クラスターアイコンの色を設置者の割合に応じて変更（オプション）

参考URL: https://github.com/Leaflet/Leaflet.markercluster
既存の機能（ポップアップ、凡例）は維持してください。」
```

**学習ポイント**:
- 外部ライブラリの導入
- プラグインのAPI理解
- パフォーマンス最適化

---

### 問題7: 統計情報パネルの追加

**目標**: 表示中の学校データの統計情報を集計して表示する。

**要件**:
- 設置者別（公立/私立）の学校数を集計
- 学校種別ごとの内訳を円グラフまたは棒グラフで可視化
- フィルター適用時は表示中のデータのみ集計
- グラフライブラリ（Chart.jsなど）を使用

**ヒント：効果的なプロンプトの書き方**:
```
「統計情報を表示するパネルを追加してください。

要件：
1. Chart.js（CDN版）を使用して以下の統計をグラフ化：
   - 設置者別（公立/私立）の円グラフ
   - 学校種別ごとの棒グラフ
2. 画面左下に統計パネルを配置（既存のlegendの位置を調整）
3. GeoJSONのfeaturesからデータを集計する関数を実装
4. グラフの色は既存のマーカー色と統一（公立=#3498db、私立=#e74c3c）

実装の流れ：
- aggregateData()関数を作成してP29_006とP29_003で集計
- Chart.jsのPieとBarタイプを使用
- レスポンシブデザインに配慮（canvas要素のサイズ指定）」
```

**学習ポイント**:
- データ集計とグルーピング（Array.reduce）
- グラフライブラリの統合
- 動的なデータ可視化

---

### 問題8: 複数年度データの比較機能

**目標**: 異なる年度のデータを切り替えて表示し、廃校や新設校を確認できるようにする。

**要件**:
- 2023年版と2021年版のGeoJSONデータを両方読み込む
- 年度切り替えスライダーまたはボタンを配置
- 廃校フラグ（P29_007）を活用して廃校をハイライト
- 年度間の差分（新設校、廃校）を色分けして表示

**ヒント：効果的なプロンプトの書き方**:
```
「複数年度のデータを比較できる機能を追加してください。

前提条件：
- 2021年版データも同様の形式で取得済みと仮定（P29-23_21.geojson）
- データソース: https://nlftp.mlit.go.jp/ksj/gml/datalist/KsjTmplt-P29-2021.html

実装要件：
1. 2つのGeoJSONファイルを並行してfetch
2. 画面上部に年度切り替えボタン（2021/2023）を配置
3. 学校コード（P29_002）をキーにして年度間でマッチング
4. 以下の状態を色分け表示：
   - 両年度に存在: 通常色
   - 2023年のみ: 緑（新設校）
   - 2021年のみ: グレー（廃校）
   - P29_007=1の廃校フラグも考慮
5. 凡例も更新して新設/廃校の色を追加

データ構造の比較にはMap()やSet()の使用を検討してください。」
```

**学習ポイント**:
- 複数の非同期データ読み込み（Promise.all）
- データの差分抽出アルゴリズム
- 時系列データの可視化

---

## 上級レベル

### 問題9: ルート検索機能

**目標**: 2つの学校間の経路を表示し、距離と所要時間を計算する。

**要件**:
- 2つの学校を選択できるUI
- Leaflet Routingプラグインを使用して経路表示
- 徒歩・自転車・車での所要時間を表示
- 経路上の情報（距離、経由地）を表示

**ヒント：効果的なプロンプトの書き方**:
```
「2つの学校間のルート検索機能を実装してください。

技術選択：
- Leaflet Routing Machineプラグインを使用
- ルーティングエンジン: OSRM（OpenStreetMap Routing Machine）

実装ステップ：
1. CDNからleaflet-routing-machine.jsとCSSを読み込み
2. サイドパネルに「出発地」「目的地」の学校選択ドロップダウンを追加
3. 学校リストはP29_004（学校名）でソート
4. 「ルート検索」ボタンクリックでL.Routing.control()を実行
5. 経路を地図上にポリラインで表示
6. 距離と推定所要時間を表示

参考：
- https://www.liedman.net/leaflet-routing-machine/
- OSRM APIは無料で使用可能（利用規約要確認）

エラーハンドリング：
- APIリクエスト失敗時のメッセージ表示
- 同じ学校を選択した場合の警告」
```

**学習ポイント**:
- 外部ルーティングAPIの統合
- 地理空間計算
- 複雑なユーザーインタラクション

---

### 問題10: ヒートマップ表示

**目標**: 学校の密度をヒートマップで可視化する。

**要件**:
- Leaflet.heatプラグインを使用
- 学校の密度を色の濃淡で表現
- マーカー表示とヒートマップ表示を切り替え可能
- ズームレベルに応じてヒートマップの粒度を調整

**ヒント：効果的なプロンプトの書き方**:
```
「学校の分布密度をヒートマップで可視化する機能を追加してください。

実装要件：
1. Leaflet.heat プラグインをCDNから読み込み
   - https://github.com/Leaflet/Leaflet.heat
2. GeoJSONの座標データをヒートマップ用の配列に変換
   - フォーマット: [[lat, lng, intensity], ...]
3. 画面右上に「マーカー表示」「ヒートマップ表示」の切り替えトグルボタンを追加
4. ヒートマップのオプション設定：
   - radius: 25
   - blur: 15
   - maxZoom: 13
   - gradient: 青→緑→黄→赤のグラデーション
5. 設置者別（公立/私立）でヒートマップを分けるオプションも追加

実装のヒント：
- L.heatLayer()でレイヤー作成
- map.addLayer() / map.removeLayer()で表示切り替え
- ボタンのスタイルは既存のinfo-panelに合わせる」
```

**学習ポイント**:
- ヒートマップの原理と可視化技術
- レイヤーの切り替え制御
- パフォーマンスを考慮したデータ変換

---

### 問題11: データダウンロード機能

**目標**: 表示中のデータやフィルター結果をCSV/GeoJSON形式でダウンロードできるようにする。

**要件**:
- ダウンロードボタンの追加
- CSV形式とGeoJSON形式を選択可能
- フィルター適用時は絞り込まれたデータのみダウンロード
- ファイル名に日時を含める

**ヒント：効果的なプロンプトの書き方**:
```
「表示中のデータをエクスポートする機能を実装してください。

機能仕様：
1. 画面右上にダウンロードボタンを追加（ドロップダウン形式）
   - 「CSV形式でダウンロード」
   - 「GeoJSON形式でダウンロード」
2. CSV形式の仕様：
   - ヘッダー行: 学校コード,学校名,所在地,設置者,緯度,経度
   - 文字コード: UTF-8 BOM付き（Excel対応）
3. GeoJSON形式：
   - 元のGeoJSON構造を維持
   - フィルター適用時はfeatures配列を絞り込み
4. ダウンロードファイル名：
   - schools_aichi_YYYYMMDD_HHMMSS.csv
   - schools_aichi_YYYYMMDD_HHMMSS.geojson

実装手順：
- JavaScript Blobオブジェクトでファイル生成
- URL.createObjectURL()で一時URLを作成
- <a>タグのdownload属性でダウンロード実行
- ダウンロード後はURL.revokeObjectURL()でメモリ解放

CSVエスケープ処理：
- カンマ、改行、ダブルクォートを含むフィールドは"で囲む
- フィールド内の"は""にエスケープ」
```

**学習ポイント**:
- Blob APIとファイルダウンロード
- データフォーマット変換
- 文字エンコーディング処理

---

## エキスパートレベル

### 問題12: オフライン対応（PWA化）

**目標**: Service Workerを使ってアプリをPWA化し、オフラインでも動作するようにする。

**要件**:
- Service Workerの実装
- キャッシュ戦略の設定
- manifest.jsonの作成
- インストール可能なアプリとして動作

**ヒント：効果的なプロンプトの書き方**:
```
「このアプリをPWA（Progressive Web App）化してオフライン対応させてください。

実装ファイル：
1. manifest.json（アプリマニフェスト）の作成
   - name: "愛知県学校マップ"
   - short_name: "学校マップ"
   - start_url: "/"
   - display: "standalone"
   - icons: 192x192と512x512のアイコン画像を指定
   - theme_color: "#3498db"

2. service-worker.jsの作成
   - キャッシュ戦略: Cache First（オフライン優先）
   - キャッシュ対象:
     * index.html
     * P29-23_23.geojson
     * Leaflet.js CSS/JS
     * OpenStreetMapタイル（一部のズームレベル）
   - バージョン管理: CACHE_VERSION定数で管理

3. index.htmlの修正
   - <head>にmanifest.jsonへのリンク追加
   - Service Worker登録スクリプトを追加
   - インストール促進バナーの実装（任意）

注意事項：
- Service WorkerはHTTPS環境が必要（localhostは除く）
- キャッシュサイズに注意（GeoJSONファイルが944KB）
- 更新時のキャッシュクリア戦略を実装

テスト方法：
- Chrome DevToolsのApplicationタブで確認
- Lighthouseでスコア測定」
```

**学習ポイント**:
- Service Worker API
- キャッシュ戦略
- PWAの概念と実装

---

### 問題13: リアルタイムコラボレーション機能

**目標**: 複数ユーザーが同時に地図を閲覧し、メモやピンを共有できる機能を追加する。

**要件**:
- WebSocketまたはFirebaseを使ったリアルタイム通信
- ユーザーが追加したカスタムマーカーを全員で共有
- チャット機能の実装
- ユーザーごとに異なる色でカーソル位置を表示

**ヒント：効果的なプロンプトの書き方**:
```
「Firebase Realtime Databaseを使用してリアルタイムコラボレーション機能を実装してください。

前提条件：
- Firebaseプロジェクトを作成済み（設定情報はfirebaseConfig変数に格納）
- Firebase SDK（CDN版）を使用

機能要件：
1. ユーザー認証（匿名認証でOK）
   - firebase.auth().signInAnonymously()
   - ユーザーにランダムな色とIDを割り当て

2. カスタムマーカーの共有
   - 地図をダブルクリックで任意の位置にマーカー追加
   - マーカー情報（位置、テキスト、色、タイムスタンプ）をFirebaseに保存
   - 他のユーザーが追加したマーカーをリアルタイム表示
   - データベース構造: /markers/{markerId}/

3. ユーザーカーソル位置の共有
   - mousemoveイベントで現在の地図座標を取得
   - Firebase /cursors/{userId}/ に座標を保存（throttle処理必須）
   - 他のユーザーのカーソルを色付き円で表示

4. 簡易チャット機能
   - 画面下部に固定チャットパネル
   - メッセージをFirebase /messages/ に保存
   - 最新50件を表示

セキュリティ：
- Firebase Rulesで読み取りは全員許可、書き込みは認証済みのみ
- XSS対策（入力値のサニタイゼーション）

パフォーマンス最適化：
- throttle/debounce処理（lodash使用推奨）
- Firebase .on()リスナーの適切な管理
- 切断時のクリーンアップ処理

注意：この機能はバックエンドを必要とするため、難易度が高いです。
まずはFirebaseの公式ドキュメントで基本的な使い方を学習してください。」
```

**学習ポイント**:
- リアルタイムデータベース
- WebSocketプロトコル
- 並行処理とデータ同期
- セキュリティとスケーラビリティ

---

## 困ったときは（デバッグの基本）

### エラーの見つけ方

1. ブラウザで `F12` キーを押して開発者ツールを開く
2. 「Console（コンソール）」タブを選ぶ
3. 赤い文字のエラーメッセージを読む（例: `Uncaught ReferenceError: xxx is not defined` → 変数名や関数名の打ち間違いが疑われます）

### よくある失敗と対処法

| 症状 | よくある原因と対処法 |
|------|--------------------|
| 画面が真っ白になった | JavaScriptの文法エラー（括弧やクォートの閉じ忘れ）。Consoleに表示される行番号を確認 |
| 変更が反映されない | ファイルの保存忘れ、またはブラウザのキャッシュ。`Ctrl + Shift + R`（Macは `Cmd + Shift + R`）で強制再読み込み |
| マーカーが全部消えた | データ読み込み処理（fetchの部分）を壊してしまった可能性。Consoleのエラーを確認 |

### 元に戻したいとき

```bash
git diff                    # 何を変更したか確認する
git checkout -- index.html  # 直近のコミット時点まで戻す（変更は破棄されるので注意）
```

### AIへの質問のしかた

エラーで行き詰まったら、以下をセットでAIに伝えると的確な回答が得られやすくなります。

1. 何をしようとしたか（例:「検索機能を追加しようとした」）
2. 実際に起きたこと（例:「画面が真っ白になった」）
3. Consoleに表示されたエラーメッセージ（コピー＆ペースト）
4. 変更したコードの該当部分

---

## プロンプト作成のベストプラクティス

### 1. 具体的な要件を明示する
❌ 悪い例：「検索機能を追加して」
✅ 良い例：「学校名（P29_004フィールド）で部分一致検索し、結果を地図上でハイライトする機能を追加して」

### 2. 既存コードとの統合を指示する
```
「既存のコードスタイルとデザインに合わせてください」
「現在のgetMarkerColor()関数を拡張する形で実装してください」
「info-panelと同じCSSスタイルを使用してください」
```

### 3. 参考資料を提示する
```
「P29-23_23_structure.mdを参照してデータ構造を理解してください」
「Leaflet.jsの公式ドキュメント（https://leafletjs.com/reference.html）を参考にしてください」
```

### 4. 実装の優先順位を明確にする
```
「以下の順序で実装してください：
1. まずUIコンポーネントを追加
2. データのフィルタリングロジックを実装
3. 最後にエラーハンドリングを追加」
```

### 5. 期待される動作を詳細に説明する
```
「ボタンをクリックすると：
1. 現在表示中のマーカーを取得
2. 座標データをCSV形式に変換
3. Blobオブジェクトを作成
4. ダウンロードを自動実行」
```

### 6. 制約条件を伝える
```
「外部ライブラリは極力避け、Vanilla JavaScriptで実装してください」
「ファイルサイズは100KB以下に抑えてください」
「IE11のサポートは不要です」
```

### 7. テスト方法を含める
```
「実装後、以下をテストしてください：
- 検索ボックスに「名古屋」と入力
- 該当する学校が赤くハイライトされることを確認
- ハイライトは5秒後に自動的に解除されることを確認」
```

---

## 参考文献

白辺 陽（2025）『WebエンジニアのためのChatGPT Webアプリ開発術 効率的なプロンプトで開発力アップ！』翔泳社

---

## さらなる学習リソース

- [Leaflet.js チュートリアル](https://leafletjs.com/examples.html)
- [GeoJSON仕様](https://geojson.org/)
- [国土数値情報 利用ガイド](https://nlftp.mlit.go.jp/ksj/)
- [MDN Web Docs - Geolocation API](https://developer.mozilla.org/ja/docs/Web/API/Geolocation_API)
- [Web.dev - Progressive Web Apps](https://web.dev/progressive-web-apps/)
